{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "claude-inbox",
  "title": "Claude Inbox",
  "description": "Every Claude Code session that is waiting for you, in one place. Approve, answer and dispatch without hunting for the terminal window.",
  "icon": "icon.png",
  "author": "andreyblack",
  "categories": [
    "Developer Tools"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "inbox",
      "title": "Claude Inbox",
      "subtitle": "Claude Code",
      "description": "Sessions waiting for a decision, sessions running, sessions finished.",
      "mode": "view"
    },
    {
      "name": "menubar",
      "title": "Claude Sessions",
      "subtitle": "Claude Code",
      "description": "Menu bar badge with the sessions that need you.",
      "mode": "menu-bar",
      "interval": "10s"
    }
  ],
  "preferences": [
    {
      "name": "inboxDir",
      "type": "textfield",
      "required": false,
      "title": "Inbox directory",
      "description": "Where the bridge hooks write state. Leave empty for ~/.claude/inbox.",
      "default": ""
    }
  ],
  "dependencies": {
    "@raycast/api": "^2.4.1",
    "@raycast/utils": "^2.3.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "@types/react": "^19.0.0",
    "typescript": "^5.9.3",
    "esbuild": "^0.28.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "lint": "ray lint",
    "fix-lint": "ray lint --fix",
    "test": "node test/run.mjs"
  }
}
